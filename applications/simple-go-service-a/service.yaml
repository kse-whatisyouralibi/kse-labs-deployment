apiVersion: v1
kind: Service
metadata:
  name: simple-go-service-a
  namespace: simple-go-service-a
  labels:
    app.kubernetes.io/name: simple-go-service-a
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: simple-go-service-a
